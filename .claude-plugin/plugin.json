{
  "name": "jev-claudecode-ssy",
  "displayName": "Jev Context Compaction · Shengsuanyun",
  "version": "1.1.3",
  "description": "Jev-guided context compaction and parallel PR review for Claude Code through Shengsuanyun.",
  "author": {
    "name": "DM010727"
  },
  "homepage": "https://github.com/DM010727/jev-claudecode-ssy",
  "repository": "https://github.com/DM010727/jev-claudecode-ssy.git",
  "license": "MIT",
  "userConfig": {
    "apiKey": {
      "type": "string",
      "title": "Shengsuanyun API key",
      "description": "Shengsuanyun API key. Stored as a masked Claude Code secret. You can alternatively set SSY_API_KEY.",
      "sensitive": true
    },
    "keepThreshold": {
      "type": "number",
      "title": "Keep threshold",
      "description": "Minimum Jev probability that a tool call or tool result is still needed; below it the item is removed.",
      "default": 0.5
    },
    "preserveRecentMessages": {
      "type": "number",
      "title": "Recent messages to preserve",
      "description": "Number of newest messages pinned from compaction.",
      "default": 6
    },
    "compactAtPercent": {
      "type": "number",
      "title": "Compaction percentage",
      "description": "Context percentage at which turn.complete requests compaction.",
      "default": 60
    },
    "minReductionRatio": {
      "type": "number",
      "title": "Minimum reduction ratio",
      "description": "Minimum estimated character reduction required to replace history.",
      "default": 0.25
    },
    "maxStateTokens": {
      "type": "number",
      "title": "Maximum Jev state tokens",
      "description": "Estimated token budget for the conversation state sent with every Jev request.",
      "default": 25000
    },
    "maxRequestTokens": {
      "type": "number",
      "title": "Maximum Jev request tokens",
      "description": "Estimated token budget for state plus questions in one Jev request.",
      "default": 30000
    },
    "truncateHeadChars": {
      "type": "number",
      "title": "Truncated tool result head",
      "description": "Characters of a tool result kept when Jev decides its full output is no longer needed; 0 keeps only the note.",
      "default": 300
    },
    "model": {
      "type": "string",
      "title": "Jev model",
      "description": "Jev model name exposed by Shengsuanyun.",
      "default": "jev-latest"
    }
  }
}
